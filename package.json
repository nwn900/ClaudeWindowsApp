{
  "name": "claude",
  "version": "1.0.0",
  "description": "claude desktop application.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "electron-builder build --win --publish never",
    "dist": "electron-builder build --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^31.2.0",
    "electron-builder": "^24.9.1"
  },
  "build": {
    "appId": "com.yourcompany.claude",
    "productName": "Claude",
    "win": {
      "signAndEditExecutable": false,
      "target": [
        "nsis"
      ],
      "icon": "icon.ico"
    }
  }
}
